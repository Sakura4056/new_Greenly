server:
  port: 8085

spring:
  application:
    name: plant-backend

  # MySQL 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_plant_official?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456


  # 文件上传限制
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

  # 邮件配置
  mail:
    host: smtp.163.com
    port: 465
    protocol: smtp
    username: sakura010903@163.com
    password: FLv2T9wTgXNZ4E3A
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            required: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory

# MyBatis Plus 配置 (全局)
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# JWT 配置
jwt:
  secret: 5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
  expiration: 86400000
  header: Authorization

# 日志
logging:
  level:
    com.plant.backend: DEBUG
    org.springframework: INFO
    com.zaxxer.hikari: INFO

# 应用特定配置
app:
  file-upload-path: ./data/uploads/
